<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.digitalcave.buddi.live.db.Users">
	<resultMap id="user" type="ca.digitalcave.buddi.live.model.User">
		<id column="id" property="id"/>
		<result column="identifier" property="identifier"/>
		<result column="credentials" property="credentials"/>
		<result column="encryption_key" property="encryptionKey"/>
		<result column="email" property="email"/>
		<result column="premium" property="premium"/>
		<result column="locale" property="locale"/>
		<result column="currency" property="currency"/>
		<result column="timezone" property="timezone"/>
		<!-- 
		<result column="show_cleared" property="showCleared"/>
		<result column="show_reconciled" property="showReconciled"/>
		<result column="date_format" property="dateFormat"/>
		-->
		<result column="show_deleted" property="showDeleted"/>
		<result column="created" property="created"/>
		<result column="modified" property="modified"/>
	</resultMap>
	
	<select id="selectUser" resultMap="user">
		SELECT 
			id,
			identifier,
			credentials,
			encryption_key,
			email,
			premium,
			locale,
			currency,
			timezone,
			show_deleted,
			created,
			modified
		FROM users 
		WHERE identifier = #{identifier}
	</select>
	
	<insert id="insertUser">
		<selectKey keyProperty="user.id" resultType="int" order="BEFORE">
			SELECT coalesce(max(id), 0) + 1 from users
		</selectKey>
		INSERT INTO users
			(
				id,
				identifier, 
				credentials,
				uuid,
				email,
				premium,
				locale,
				currency,
				timezone,
				show_deleted,
				show_reconciled,
				show_cleared,
				created, 
				modified
			)
		VALUES
			(
				#{user.id}, 
				#{user.identifier},
				#{user.credentials},
				#{user.uuid}, 
				#{user.email,jdbcType=VARCHAR},
				'N',
				#{user.locale},
				#{user.currency},
				#{user.timezone},
				'Y',
				'N',
				'N',
				CURRENT_TIMESTAMP, 
				CURRENT_TIMESTAMP
			)
	</insert>
	
	<update id="updateUser">
		UPDATE users
		SET
			<if test="user.credentials != null">credentials = #{user.credentials},</if>
			email = #{user.email,jdbcType=VARCHAR},
			locale = #{user.locale},
			currency = #{user.currency.toString(),jdbcType=VARCHAR},
			timezone = #{user.timezone,jdbcType=VARCHAR},
			show_deleted = <choose><when test="user.showDeleted == true">'Y'</when><otherwise>'N'</otherwise></choose>,
			modified = CURRENT_TIMESTAMP
		WHERE identifier = #{user.identifier}
	</update>
	
	<update id="updateUserLoginTime">
		UPDATE users
		SET
			last_login = CURRENT_TIMESTAMP
		WHERE identifier = #{user.identifier}
	</update>
	
	<update id="updateUserEncryptionKey">
		UPDATE users
		SET
			encryption_key = #{user.encryptionKey,jdbcType=VARCHAR},
			modified = CURRENT_TIMESTAMP
		WHERE identifier = #{user.identifier}
	</update>
	
	<delete id="deleteUser">
		DELETE FROM users
		WHERE identifier = #{user.identifier}
	</delete>
</mapper>