<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.digitalcave.buddi.live.db.Users">
	<select id="selectUser" resultType="ca.digitalcave.buddi.live.model.User">
		SELECT 
			id,
			identifier,
			credentials,
			email,
			locale,
			premium,
			created,
			modified
		FROM users 
		WHERE identifier = #{identifier}
	</select>
	
	<insert id="insertUser">
		<selectKey keyProperty="user.id" resultType="int" order="BEFORE">
			SELECT coalesce(max(id), 0) + 1 from users
		</selectKey>
		INSERT INTO users
			(id, identifier, credentials, uuid, email, locale, premium, created, modified)
		VALUES
			(#{user.id}, #{user.identifier}, #{user.credentials}, #{user.uuid}, #{user.email,jdbcType=VARCHAR}, #{user.locale,jdbcType=VARCHAR}, <choose><when test="user.premium == true">'Y'</when><otherwise>'N'</otherwise></choose>, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
	</insert>
	
	<update id="updateUser">
		UPDATE users
		SET
			<if test="user.credentials != null">credentials = #{user.credentials},</if>
			<if test="user.email != null">email = #{user.email},</if>
			<if test="user.locale != null">locale = #{user.locale},</if>
			premium = <choose><when test="user.premium == true">'Y'</when><otherwise>'N'</otherwise></choose>,
			modified = CURRENT_TIMESTAMP
		WHERE identifier = #{user.identifier}
	</update>
	
	<delete id="deleteUser">
		DELETE FROM users
		WHERE identifier = #{user.identifier}
	</delete>
</mapper>