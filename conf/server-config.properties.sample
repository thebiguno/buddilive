#Sample for Postgresql connection.  Change the connection details as needed for your server.
db.driver=org.postgresql.Driver
db.url=jdbc:postgresql:buddilive
db.user=buddilive
db.password=password
db.query=select 1

#Private servers are for bundled apps, and enable / disable various UI options.  Defaults to true.
server.private=false

#Mail configuration.  Change as needed
#mail.smtp.from=user@localhost
#mail.smtp.host=localhost
#mail.smtp.port=25
#mail.smtp.auth=false
#mail.smtp.username=user
#mail.smtp.password=password
#mail.smtp.starttls.enable=false